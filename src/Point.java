public class Point {
    public int x;
    public int y;
    public int weight; // Add this line

    public Point(int x, int y, int weight) {
        this.x = x;
        this.y = y;
        this.weight = weight; // Initialize weight
    }

    // Getters and setters for x, y, and weight (optional)
}
